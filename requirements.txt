flake8>=6.0.0,<7.0.0          # 静态代码检查（检测语法错误、代码规范问题）
pylint>=2.17.0,<3.0.0         # 深度代码质量分析（检测逻辑冗余、命名不规范等）

# 测试工具（比 unittest 更灵活，支持覆盖率）
pytest>=7.3.1,<8.0.0          # 高级测试框架（兼容 unittest 用例，支持参数化、夹具等）
pytest-cov>=4.1.0,<5.0.0      # 测试覆盖率报告生成（配合 pytest 使用）

# 代码格式化工具（保持团队代码风格一致）
black>=23.3.0,<24.0.0         # 自动代码格式化（强制统一代码风格，无配置负担）
isort>=5.12.0,<6.0.0          # 自动导入排序（规范 import 顺序，避免冲突）
